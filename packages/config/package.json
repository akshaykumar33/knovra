{
  "name": "@knovra/config",
  "version": "0.1.0",
  "description": "Environment and knovra.yaml configuration schemas and validation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js || echo 'No tests yet'"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
