{
  "name": "@knovra/contracts",
  "version": "0.1.0",
  "description": "Shared types, schemas and protobuf contracts for Knovra",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js || echo 'No compiled tests to run yet'"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
